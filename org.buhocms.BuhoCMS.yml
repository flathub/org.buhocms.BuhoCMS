app-id: org.buhocms.BuhoCMS
runtime: org.freedesktop.Platform
runtime-version: '22.08'
sdk: org.freedesktop.Sdk
command: buhocms
finish-args:
  - --share=ipc
  - --socket=fallback-x11
  - --socket=wayland
  - --device=dri
  - --share=network
  - --filesystem=host
modules:
  - name: BuhoCMS
    buildsystem: simple
    build-commands:
      - mkdir -p /app/BuhoCMS
      - cp -r buhocms data lib /app/BuhoCMS
      - chmod +x /app/BuhoCMS/buhocms
      - mkdir -p /app/bin
      - ln -s /app/BuhoCMS/buhocms /app/bin/buhocms
      - install -D installers/Flatpak/org.buhocms.BuhoCMS.svg /app/share/icons/hicolor/scalable/apps/${FLATPAK_ID}.svg
      - install -D installers/Flatpak/org.buhocms.BuhoCMS.desktop -t /app/share/applications
      - install -D installers/Flatpak/org.buhocms.BuhoCMS.metainfo.xml -t /app/share/metainfo
    sources:
      - type: git
        url: https://github.com/iakmds/buhocms.git
        tag: v0.6.2
        commit: a1e5da4f9992b1e77231114f7b7e202ab842038b
      - type: archive
        url: https://github.com/iakmds/buhocms/releases/download/v0.6.2/BuhoCMS-Linux-Portable-0.6.2.tar.gz
        sha256: 750b637bfea28bc2949e4bdac2a940d4ce3b5d80c46f34b70d1db95410d9466f
        strip-components: 0
